#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: writes the string s and a trailing newline to standard output
 * Return: Always 0 (success)
 *
 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle, \n");
	return (0);
}

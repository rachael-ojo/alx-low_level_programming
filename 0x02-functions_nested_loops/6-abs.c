#include "main.h"
#include <stdio.h>

/**
 * _abs - Computes the absolute value of an integer
 * @n: The integer to be computed
 * Return: The absolute value
 */
int _abs(int n)
{
	int result = abs(n);

	return (result);
}

int main(void)
{
}
